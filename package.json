{
  "name": "opencode",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "imports": {
    "#libs/*": "./libs/dist/*"
  },
  "scripts": {
    "build:libs": "npx tsc -p libs/tsconfig.json",
    "build:tools": "npx tsc -p tools/tsconfig.json",
    "build:plugins": "npx tsc -p plugins/tsconfig.json",
    "build:mcps": "npx tsc -p mcps/tsconfig.json",
    "build": "npm run build:libs && npm run build:tools && npm run build:mcps && npm run build:plugins",
    "clean": "npx tsx scripts/clean.ts",
    "rebuild": "npm run clean && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@opencode-ai/plugin": "1.4.0",
    "better-sqlite3": "^12.8.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^25.5.2"
  }
}