{
  "name": "opencode",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc -p plugins/tsconfig.json",
    "clean": "npx tsx scripts/clean.ts",
    "rebuild": "npm run clean && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:checker": "tsx --test plugins/test/security-checker.test.ts"
  },
  "dependencies": {
    "@opencode-ai/plugin": "1.3.13"
  },
  "devDependencies": {
    "@types/node": "^25.5.2"
  }
}