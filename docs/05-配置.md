# OpenCode 配置

OpenCode 使用 JSON 配置文件进行配置，支持 JSON 和 JSONC（带注释）格式。

## 配置文件位置

配置文件按以下优先级**合并**（后面的覆盖前面的）：

| 优先级 | 位置 | 说明 |
|--------|------|------|
| 1 | `.well-known/opencode` | 远程组织配置 |
| 2 | `~/.config/opencode/opencode.json` | 全局用户配置 |
| 3 | `OPENCODE_CONFIG` 环境变量 | 自定义配置路径 |
| 4 | `opencode.json`（项目根目录） | 项目配置 |
| 5 | `.opencode/` 目录 | 代理、命令、插件 |
| 6 | `OPENCODE_CONFIG_CONTENT` 环境变量 | 内联配置 |

### 配置文件目录

子目录使用**复数名称**：`agents/`、`commands/`、`modes/`、`plugins/`、`skills/`、`tools/`、`themes/`

## 配置格式

```jsonc
{
  "$schema": "https://opencode.ai/config.json",
  "model": "anthropic/claude-sonnet-4-5",
  "autoupdate": true,
  "server": {
    "port": 4096
  }
}
```

## 配置选项详解

### TUI 配置

```json
{
  "tui": {
    "scroll_speed": 3,
    "scroll_acceleration": {
      "enabled": true
    },
    "diff_style": "auto"
  }
}
```

- `scroll_acceleration.enabled` - macOS 风格滚动加速
- `scroll_speed` - 滚动速度（最小值 1）
- `diff_style` - 差异渲染方式：`"auto"` 或 `"stacked"`

### 服务器配置

```json
{
  "server": {
    "port": 4096,
    "hostname": "0.0.0.0",
    "mdns": true,
    "mdnsDomain": "myproject.local",
    "cors": ["http://localhost:5173"]
  }
}
```

### 工具配置

```json
{
  "tools": {
    "write": false,
    "bash": false
  }
}
```

### 模型配置

```json
{
  "model": "anthropic/claude-sonnet-4-5",
  "small_model": "anthropic/claude-haiku-4-5",
  "provider": {
    "anthropic": {
      "options": {
        "timeout": 600000,
        "setCacheKey": true
      }
    }
  }
}
```

### 主题配置

```json
{
  "theme": ""
}
```

### 代理配置

```json
{
  "agent": {
    "code-reviewer": {
      "description": "Reviews code for best practices",
      "model": "anthropic/claude-sonnet-4-5",
      "prompt": "You are a code reviewer...",
      "tools": {
        "write": false,
        "edit": false
      }
    }
  }
}
```

详见 [15-代理.md](./15-代理.md)

### 默认代理

```json
{
  "default_agent": "plan"
}
```

### 分享配置

```json
{
  "share": "manual"  // "manual" | "auto" | "disabled"
}
```

### 命令配置

```json
{
  "command": {
    "test": {
      "template": "Run tests with coverage...",
      "description": "Run tests with coverage",
      "agent": "build",
      "model": "anthropic/claude-haiku-4-5"
    }
  }
}
```

详见 [16-命令.md](./16-命令.md)

### 快捷键配置

```json
{
  "keybinds": {}
}
```

详见 [19-快捷键.md](./19-快捷键.md)

### 自动更新

```json
{
  "autoupdate": false  // 或 "notify"
}
```

### 格式化工具配置

```json
{
  "formatter": {
    "prettier": {
      "disabled": true
    },
    "custom-prettier": {
      "command": ["npx", "prettier", "--write", "$FILE"],
      "environment": {
        "NODE_ENV": "development"
      },
      "extensions": [".js", ".ts", ".jsx", ".tsx"]
    }
  }
}
```

详见 [20-格式化工具.md](./20-格式化工具.md)

### 权限配置

```json
{
  "permission": {
    "edit": "ask",
    "bash": "ask"
  }
}
```

详见 [17-权限.md](./17-权限.md)

### 压缩配置

```json
{
  "compaction": {
    "auto": true,
    "prune": true,
    "reserved": 10000
  }
}
```

### 文件监视器

```json
{
  "watcher": {
    "ignore": ["node_modules/**", "dist/**", ".git/**"]
  }
}
```

### MCP 服务器

```json
{
  "mcp": {}
}
```

详见 [22-MCP服务器.md](./22-MCP服务器.md)

### 插件配置

```json
{
  "plugin": ["opencode-helicone-session", "@my-org/custom-plugin"]
}
```

详见 [25-插件.md](./25-插件.md)

### 指令文件

```json
{
  "instructions": ["CONTRIBUTING.md", "docs/guidelines.md", ".cursor/rules/*.md"]
}
```

详见 [14-规则.md](./14-规则.md)

### 禁用/启用提供商

```json
{
  "disabled_providers": ["openai", "gemini"]
}
// 或
{
  "enabled_providers": ["anthropic", "openai"]
}
```

## 变量替换

### 环境变量

```json
{
  "model": "{env:OPENCODE_MODEL}",
  "provider": {
    "anthropic": {
      "options": {
        "apiKey": "{env:ANTHROPIC_API_KEY}"
      }
    }
  }
}
```

### 文件内容

```json
{
  "instructions": ["./custom-instructions.md"],
  "provider": {
    "openai": {
      "options": {
        "apiKey": "{file:~/.secrets/openai-key}"
      }
    }
  }
}
```

## Schema 验证

配置文件的 Schema 定义在 `https://opencode.ai/config.json`，编辑器应能基于此提供验证和自动补全。
